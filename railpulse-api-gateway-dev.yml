server:
  port: 8080

spring:
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: fleet-service
              uri: lb://railpulse-fleet-service
              predicates:
                - Path=/api/v1/locomotives/**

            - id: telemetry-service
              uri: lb://railpulse-telemetry-service
              predicates:
                - Path=/api/v1/telemetry/**

management:
  endpoint:
    gateway:
      access: read-only
  endpoints:
    web:
      exposure:
        include: health,info,gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
